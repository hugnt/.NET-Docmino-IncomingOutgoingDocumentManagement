@model Docmino.Application.Models.External.Email.ApprovalNotificationEmail
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Thông báo phê duyệt văn bản</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            font-family: Arial, sans-serif;
            color: #333;
            line-height: 1.5;
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            border: 2px solid #e0e0e0;
        }

        .header {
            background-color: #2c3e50;
            color: #ffffff;
            padding: 30px;
            text-align: center;
        }

            .header h1 {
                margin: 0 0 8px 0;
                font-size: 20px;
                font-weight: 600;
                letter-spacing: 1px;
            }

        .header-subtitle {
            margin: 0;
            font-size: 14px;
            opacity: 0.9;
            font-weight: 400;
        }

        .content {
            padding: 30px;
        }

        .greeting {
            font-size: 16px;
            margin-bottom: 20px;
            color: #333;
        }

        .notification-box {
            background-color: #f8f9fa;
            border: 2px solid #4a90e2;
            border-left: 6px solid #4a90e2;
            padding: 20px;
            margin: 20px 0;
        }

        .notification-title {
            font-weight: 600;
            color: #4a90e2;
            margin-bottom: 8px;
            font-size: 14px;
        }

        .notification-text {
            color: #666;
            font-size: 14px;
            line-height: 1.5;
            margin: 0;
        }

        .info-section {
            margin: 30px 0;
        }

        .info-title {
            background-color: #f1f3f4;
            padding: 15px;
            margin: 0;
            font-size: 16px;
            font-weight: 600;
            color: #333;
            text-align: center;
            border: 2px solid #e0e0e0;
            border-bottom: 1px solid #e0e0e0;
        }

        .info-table {
            border: 2px solid #e0e0e0;
            border-top: none;
            width: 100%;
        }

        .info-row {
            display: table;
            width: 100%;
            border-bottom: 1px solid #f0f0f0;
        }

            .info-row:last-child {
                border-bottom: none;
            }

        .info-label {
            display: table-cell;
            background-color: #fafafa;
            padding: 12px 15px;
            font-size: 13px;
            color: #666;
            font-weight: 500;
            width: 140px;
            border-right: 1px solid #f0f0f0;
            vertical-align: middle;
        }

        .info-value {
            display: table-cell;
            padding: 12px 15px;
            font-size: 14px;
            color: #333;
            font-weight: 500;
            vertical-align: middle;
        }

        .priority-high {
            display: inline-block;
            background-color: #dc3545;
            color: white;
            padding: 4px 12px;
            font-size: 11px;
            font-weight: 600;
            border: 1px solid #dc3545;
        }

        .warning-box {
            background-color: #fff3cd;
            border: 2px solid #ffc107;
            padding: 15px;
            margin: 25px 0;
            text-align: center;
        }

        .warning-text {
            color: #856404;
            font-size: 13px;
            margin: 0;
            font-weight: 500;
        }

        .action-section {
            text-align: center;
            margin: 30px 0;
        }

        .action-button {
            display: inline-block;
            background-color: #4a90e2;
            color: #ffffff;
            padding: 15px 35px;
            text-decoration: none;
            font-weight: 600;
            font-size: 14px;
            border: 2px solid #4a90e2;
        }

            .action-button:hover {
                background-color: #357abd;
                border-color: #357abd;
            }

        .footer {
            background-color: #f8f9fa;
            padding: 20px;
            text-align: center;
            border-top: 2px solid #e0e0e0;
            font-size: 12px;
            color: #666;
        }

            .footer a {
                color: #4a90e2;
                text-decoration: none;
            }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 style="color:white;">THÔNG BÁO PHÊ DUYỆT VĂN BẢN</h1>
            <p class="header-subtitle">Hệ thống quản lý văn bản điện tử</p>
        </div>

        <div class="content">
            <p class="greeting">Kính gửi <strong>@Model.ReviewerName</strong>,</p>

            <div class="notification-box">
                <div class="notification-title">Thông báo quan trọng:</div>
                <p class="notification-text">
                    Bạn có một văn bản mới cần được xem xét và phê duyệt trong hệ thống.
                    Vui lòng kiểm tra thông tin chi tiết bên dưới và thực hiện phê duyệt theo đúng quy trình.
                </p>
            </div>

            <div class="info-section">
                <div class="info-title">THÔNG TIN VĂN BẢN</div>
                <div class="info-table">
                    <div class="info-row">
                        <div class="info-label">TÊN VĂN BẢN</div>
                        <div class="info-value">@Model.DocumentName</div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">SỐ KÝ HIỆU</div>
                        <div class="info-value">@Model.CodeNotation</div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">LOẠI VĂN BẢN</div>
                        <div class="info-value">@Model.DocumentTypeName</div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">CẤP DUYỆT</div>
                        <div class="info-value">Bước @Model.CurrentStepNumber</div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">KIỂU KÝ</div>
                        <div class="info-value">@Model.SignTypeName</div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">MỨC ĐỘ ƯU TIÊN</div>
                        <div class="info-value">
                            <span class="priority-high">@Model.UrgentPriorityName</span>
                        </div>
                    </div>
                    <div class="info-row">
                        <div class="info-label">THỜI GIAN THỰC HIỆN</div>
                        <div class="info-value">
                            <strong>Từ:</strong> @Model.StartDate &nbsp;&nbsp; <strong>Đến:</strong> @Model.EndDate
                        </div>
                    </div>
                </div>
            </div>

            <div class="warning-box">
                <p class="warning-text">
                    ⚠️ Vui lòng thực hiện phê duyệt trong thời gian quy định để đảm bảo quy trình xử lý văn bản diễn ra thuận lợi
                </p>
            </div>

            <div class="action-section">
                <a href="@Model.ActionUrl" target="_blank" class="action-button">
                    XEM & PHÊ DUYỆT NGAY
                </a>
            </div>
        </div>

        <div class="footer">
            <p>
                Hỗ trợ kỹ thuật: <a href="mailto:@Model.SupportEmail">@Model.SupportEmail</a><br>
                © 2024 Hệ thống Docmino - Quản lý văn bản điện tử
            </p>
        </div>
    </div>
</body>
</html>
